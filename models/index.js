const User = require('./User');
const Comment = require('./Comment'); // Note: need to update if we adjust this model to something different (i.e: Pins)
const Constellation = require('./Constellation');

// List associations below 